<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.maydaybonito.basicspanishfortourist">

    <application
        android:allowBackup="true"
        android:icon="@drawable/minilogoapp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".VocabTypeActivity" />
        <activity android:name=".GrammarTypeActivity" />
        <activity android:name=".ConverTypeActivity" />
        <activity android:name=".ExerciseTypeActivity" />
        <activity android:name=".ScoreActivity" />
        <activity android:name=".SettingTypeActivity" />
        <activity android:name=".RegisterActivity"></activity>
    </application>

</manifest>